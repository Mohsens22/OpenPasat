<v:NavigationViewBase
    x:Class="UnoTest.Shared.Views.NavigationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UnoTest.Shared.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:rxuno="using:ReactiveUI.Uno"
    xmlns:v="using:UnoTest.Shared.Views"
    xmlns:vm="using:UnoTest.Shared.ViewModels"
    xmlns:um="using:UnoTest.Shared.UserModels"
    xmlns:Converters="using:UnoTest.Shared.Converters"
    Background="{StaticResource AppChromeAcrylicHostBackdropMediumLowBrush}">

    <Pivot ItemsSource="{x:Bind ViewModel.NavigationItems,Mode=OneWay}"
           SelectedItem="{x:Bind ViewModel.SelectedNavigationItem}"
           >

        <Pivot.HeaderTemplate>
            <DataTemplate>
                <TextBlock Text="{Binding Title}"/>
            </DataTemplate>
        </Pivot.HeaderTemplate>

        <Pivot.ItemTemplate>
            <DataTemplate >
                <rxuno:ViewModelViewHost ViewModel="{Binding ViewModelType}"/>
            </DataTemplate>
        </Pivot.ItemTemplate>

    </Pivot>


</v:NavigationViewBase>
